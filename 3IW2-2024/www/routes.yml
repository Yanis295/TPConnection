/:
  controller: Main
  action: home

/page:
  controller: Page
  action: show


/register:
  controller: User
  action: register

/login:
  controller: User
  action: login

/logout:
  controller: User
  action: logout

/dashboard:
  controller: Backoffice
  action: dashboard
